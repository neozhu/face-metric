# Face Compare Web

Next.js dark‑first UI for uploading two faces and comparing match score.

## Env
Create `apps/web/.env.local` if the API is not on localhost:
```
NEXT_PUBLIC_API_BASE=http://localhost:8000
```

## Run
```bash
npm install
npm run dev
```

