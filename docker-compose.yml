services:
  face-metric:
    container_name: face-metric
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - deepface-cache:/root/.deepface
    restart: unless-stopped

volumes:
  deepface-cache:
    name: face-metric-weights
